module.exports = {
    theme: {
      extend: {},
    },
    variants: {
      extend: {
        ringWidth: ['focus'], // Ensure focus variant is enabled for ringWidth
        ringColor: ['focus'], // Ensure focus variant is enabled for ringColor
      },
    },
    plugins: [],
  };